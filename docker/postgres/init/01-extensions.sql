-- =============================================================================
-- PostgreSQL 초기화 스크립트
-- =============================================================================
-- 이 파일은 컨테이너 최초 기동 시 1회만 실행됩니다.
-- (PGDATA 볼륨이 비어있을 때만 동작)
-- =============================================================================

-- 시간 겹침 EXCLUDE 제약을 위한 확장
-- (Booking 테이블에서 동일 회의실의 시간 충돌을 DB 레벨에서 차단하기 위해 필요)
CREATE EXTENSION IF NOT EXISTS btree_gist;

-- UUID 생성 함수 (Prisma는 애플리케이션 레벨에서 UUID 생성하지만,
-- DB 직접 작업이나 시드 스크립트에서 유용)
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- 시간대 확인용
SET TIMEZONE = 'Asia/Seoul';
